<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <!-- Required by the Google Pay API; merged into the consuming app's
             manifest so merchants get Google Pay without any manifest work. -->
        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

        <activity
            android:name=".internal.ui.PaymentActivity"
            android:exported="false"
            android:theme="@style/Theme.PayCross.Payment" />
    </application>
</manifest>
